name: Deploy Migrations to Staging

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: sbp_e1b6b3626881dd542d8db17481d60d24de2a8a6b
      SUPABASE_DB_PASSWORD: Test_123#3211
      SUPABASE_PROJECT_ID: raqucpknugnsvlzemkgj

    steps:
      - uses: actions/checkout@v3

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase link --project-ref $SUPABASE_PROJECT_ID
      - run: supabase db push
